installing Locally
npm install typescript --save-dev
create ts file 
npx tsc app.ts              ===> This will create js file 
npx tsc app.ts --watch      ===> This will create js file  everytime the ts file changes
=============================================================================================
=============================================================================================
=============================================================================================
=============================================================================================
npm install -g typescript

Create ts file :: index.ts
```js
function greet(name: string): string {
  return `Hello, ${name}`;
}
const user: string = "Riyaz";
console.log(greet(user));
```
npx tsc app.ts
===========================================================================================
===========================================================================================
===========================================================================================
===========================================================================================
npx tsc --init
npx tsc
node index.js

